# 安装完成指南

DSH Guard 已准备就绪。

## 启动前

1. 复制 `.env.example` -> `.env`
2. 在 `.env` 中填入 `DEEPSEEK_API_KEY`
3. 确保已执行 `pnpm install`

## 启动

```sh
pnpm start
```

或双击 `start.bat`。

## 访问

浏览器打开 [http://127.0.0.1:3080](http://127.0.0.1:3080)。
