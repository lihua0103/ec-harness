/**
 * 已迁移：验收矩阵重写为 data-guard.test.ts（2026-08-28 两规则 + 宿主开关口径）。
 * 本文件保留为退役标记；Windows 侧可直接删除（G: 挂载沙箱无法 unlink）。
 */
import { describe, it, expect } from 'vitest'

describe('退役标记', () => {
  it('旧 redaction 测试已迁移至 data-guard.test.ts', () => {
    expect(true).toBe(true)
  })
})
