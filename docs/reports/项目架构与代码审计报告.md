# DSH Guard 项目架构与代码审计报告

**审计日期**: 2026-08-25  
**审计范围**: DSH Guard 临床数据守护系统  
**项目版本**: 0.1.0  
**审计人员**: Claude  

---

## 执行摘要

本次审计对 DSH Guard 项目进行了全面的架构分析和代码质量评估。项目是一个基于 DeepSeek Harness 的企业级 AI Agent 框架，专注于临床试验数据安全和 Listing 生成。

**总体评估**: ⚠️ **中等风险 - 需要改进**

**核心发现**:
- ✅ TypeScript 核心功能完整且测试通过（34/34）
- ⚠️ 存在多个架构设计问题
- ⚠️ 代码质量参差不齐
- ❌ 部分关键功能未实现
- ⚠️ 安全机制不完整

---

## 1. 项目概览

### 1.1 项目结构

```
dsh-guard/
├── packages/enterprise/
│   ├── auth/                    # 认证插件（基础框架，未完整实现）
│   └── clinical-guard/          # 临床数据守护插件（主要功能）
├── configs/                      # Cordis 配置
├── docs/                         # 文档
├── scripts/                      # 构建脚本
└── [配置文件]
```

### 1.2 技术栈

- **语言**: TypeScript (前端), Python (后端)
- **框架**: Cordis 4.0.1, DeepSeek Harness 0.1.1-rc.2
- **包管理**: pnpm workspace (monorepo)
- **测试**: Vitest 4.1.8
- **构建**: TypeScript Compiler 6.0.0

### 1.3 代码规模

| 模块 | TypeScript 行数 | Python 行数 | 测试覆盖率 |
|------|----------------|------------|-----------|
| clinical-guard/src | ~1,220 行 | N/A | 100% (TS) |
| clinical-guard/python | N/A | ~5,000+ 行 | 部分 |
| auth | ~55 行 | N/A | 0% |

---

## 2. 架构审计

### 2.1 整体架构 ⚠️

**设计模式**: 插件化架构（基于 Cordis）

**评估**: 中等

**优点**:
- ✅ 清晰的模块划分
- ✅ 符合 Cordis 插件规范
- ✅ 支持配置热更新

**问题**:
1. **TypeScript 配置错误** ❌
   ```typescript
   // tsconfig.json
   "include": ["packages/*/src/**/*"]  // 路径配置错误
   ```
   导致根目录 TypeScript 编译失败

2. **依赖版本不一致** ⚠️
   - auth 插件引用 `Schema` 但 Cordis 4.0.1 已移除此导出
   - 存在 API 不兼容问题

3. **Python-TypeScript 集成松散** ⚠️
   - Python 后端与 TS 前端通过进程间通信
   - 缺少明确的接口契约
   - 错误处理不完整

### 2.2 clinical-guard 插件架构 ✅

**评估**: 良好

**架构分层**:
```
Service Layer (service.ts)
    ↓
Core Modules
    ├── EgressSwitch (数据出域控制)
    ├── HeaderDetector (表头检测)
    ├── EdcFieldDetector (EDC 字段识别)
    └── ListingTemplateManager (模板管理)
```

**优点**:
- ✅ 职责清晰，单一责任原则
- ✅ 依赖注入设计
- ✅ 完整的配置系统

**问题**:
- ⚠️ 缺少抽象接口层
- ⚠️ 模块间耦合度偏高（通过 Context 直接访问）

### 2.3 配置管理 ✅

**评估**: 良好

**配置层次**:
1. 环境变量 (.env)
2. Cordis 配置 (cordis.yml)
3. 默认配置 (DEFAULT_CONFIG)

**优点**:
- ✅ 支持多层配置覆盖
- ✅ 类型安全的配置接口
- ✅ 热更新支持

---

## 3. 代码质量审计

### 3.1 TypeScript 代码质量

#### 3.1.1 类型安全 ✅

**评估**: 优秀

**优点**:
- ✅ 完整的类型定义
- ✅ 严格模式开启 (`strict: true`)
- ✅ 正确的模块扩展声明

**示例** (良好实践):
```typescript
declare module '@deepseek-ai/cordis' {
  interface Context {
    clinicalGuard: ClinicalGuardService
  }
}
```

#### 3.1.2 代码组织 ✅

**评估**: 良好

**优点**:
- ✅ 清晰的文件命名
- ✅ 合理的模块划分
- ✅ 一致的代码风格

#### 3.1.3 错误处理 ⚠️

**评估**: 需要改进

**问题**:

1. **不一致的错误处理策略**
```typescript
// egress-switch.ts:136 - 过于宽泛的捕获
} catch (error) {
  this.ctx.logger.error('拦截检查失败', error)
  // 失败时拒绝（安全优先）- 但可能导致误拦截
  return {
    allowed: false,
    reason: `拦截检查失败: ${error instanceof Error ? error.message : '未知错误'}`,
    ...
  }
}
```

2. **缺少输入验证**
```typescript
// service.ts:58 - 没有验证 egressType 和 data
public async checkEgress(egressType: EgressType, data: any) {
  return this.egressSwitch.intercept(egressType, data)
}
```

3. **Python 调用缺少超时**
```typescript
// header-detector.ts:88 - spawn 没有超时控制
const python = spawn(this.pythonPath, ['-m', 'security.header_detect'], {
  cwd: process.cwd(),
})
// 如果 Python 进程挂起，会永久阻塞
```

### 3.2 Python 代码质量

#### 3.2.1 安全机制 ✅

**评估**: 良好

**优点**:
- ✅ HMAC 签名验证（防伪造）
- ✅ 完整的审计日志
- ✅ 多层防御策略

**示例** (良好实践):
```python
# egress_checkpoint.py:52
def _sign_clinical_guard(marker: str, critical_fields: dict) -> str:
    """HMAC 签名防伪造"""
    if not _EMERALD_SIGNING_SALT:
        return ""
    canonical = json.dumps(critical_fields, sort_keys=True, ensure_ascii=False)
    raw = f"{marker}:{canonical}".encode("utf-8")
    return hmac.new(_EMERALD_SIGNING_SALT, raw, hashlib.sha256).hexdigest()[:16]
```

#### 3.2.2 模式匹配 ⚠️

**评估**: 需要审查

**问题**:
- ⚠️ 硬编码的模式列表（可能产生误报/漏报）
- ⚠️ 缺少模式更新机制

---

## 4. 功能完整性审计

### 4.1 已实现功能 ✅

#### 4.1.1 数据出域控制 (EgressSwitch)

**状态**: ✅ 已实现并测试

**功能**:
- ✅ 全局开关控制
- ✅ SAS 数据集出域拦截
- ✅ Spec 数据出域拦截
- ✅ 配置热更新

**测试**: 10/10 通过

**问题**:
```typescript
// egress-switch.ts:182-201
// ⚠️ 标记为 TODO，实际拦截逻辑未实现
private async interceptSasDataset(data: any, timestamp: number): Promise<InterceptionResult> {
  // TODO: 实现 SAS 数据集拦截逻辑
  // 1. 检查数据格式
  // 2. 如果启用 AI，调用 AI 决策
  // 3. 返回拦截结果
  
  // 临时实现：基础检查
  return {
    allowed: true,  // ❌ 永远返回 true！
    reason: 'SAS 数据集检查通过',
    timestamp,
    egressType: EgressType.SAS_DATASET,
  }
}
```

**风险评级**: 🔴 **高风险 - 核心功能未实现**

#### 4.1.2 Listing 模板系统

**状态**: ✅ 完全实现

**功能**:
- ✅ 5 种标准临床模板
- ✅ 自定义模板支持
- ✅ 数据过滤和排序
- ✅ 样式规范

**测试**: 9/9 通过

**评估**: 优秀

#### 4.1.3 EDC 字段识别

**状态**: ✅ 完全实现

**功能**:
- ✅ 支持 Medidata/Oracle/Veeva
- ✅ 智能评分机制
- ✅ 不区分大小写匹配

**测试**: 7/7 通过

**评估**: 良好

#### 4.1.4 表头检测

**状态**: ⚠️ 框架完成，功能未实现

**问题**:
```typescript
// header-detector.ts:67-78
private async detectWithRules(data: any): Promise<HeaderDetectionResult> {
  this.ctx.logger.debug('使用规则检测')
  
  // 简单的规则检测逻辑
  // TODO: 实现更复杂的规则  // ❌ 未实现
  
  return {
    detected: false,  // ❌ 永远返回 false
    headers: [],
    confidence: 0,
    method: 'rule-based',
  }
}
```

**风险评级**: 🟡 **中等风险 - 功能不完整**

### 4.2 未实现功能 ❌

#### 4.2.1 认证插件 (auth)

**状态**: ❌ 仅有框架

**缺失**:
```typescript
// auth/src/index.ts:36-40
async authenticate(username: string, password: string) {
  this.ctx.logger.info(`用户认证: ${username}`)
  // TODO: 实现具体认证逻辑  // ❌ 完全未实现
  return { success: true, username }
}
```

**问题**:
- ❌ 无实际认证逻辑
- ❌ 无密码验证
- ❌ 无会话管理
- ❌ 无 LDAP/OAuth2/SAML 实现

**风险评级**: 🔴 **高风险 - 认证形同虚设**

#### 4.2.2 自定义 Listing 模板加载

**状态**: ❌ 未实现

```typescript
// listing-template.ts:176-183
private loadCustomTemplates(paths: string[]) {
  if (paths.length === 0) {
    return
  }
  
  this.ctx.logger.info(`加载 ${paths.length} 个自定义模板`)
  // TODO: 从文件加载自定义模板  // ❌ 未实现
}
```

---

## 5. 安全审计

### 5.1 安全优点 ✅

1. **Python 后端安全机制完善**
   - ✅ HMAC 签名验证
   - ✅ 审计日志
   - ✅ 多层防御

2. **类型安全**
   - ✅ TypeScript 严格模式
   - ✅ 完整类型定义

3. **配置安全**
   - ✅ 敏感信息通过环境变量
   - ✅ .env 文件在 .gitignore 中

### 5.2 安全问题 ❌

#### 5.2.1 认证缺失 🔴 高危

**问题**: 认证插件完全未实现

**影响**:
- 任何人都可以访问系统
- 无法追踪用户行为
- 无法实施访问控制

**建议**: 立即实现基础认证

#### 5.2.2 核心拦截功能未实现 🔴 高危

**问题**: `interceptSasDataset` 和 `interceptSpecData` 只是占位符

**影响**:
- 所有数据出域请求都会被放行
- 数据安全开关形同虚设
- 无法防止敏感数据泄露

**建议**: 这是核心功能，必须立即实现

#### 5.2.3 输入验证不足 🟡 中危

**问题**:
```typescript
// service.ts - 没有验证输入
public async checkEgress(egressType: EgressType, data: any) {
  return this.egressSwitch.intercept(egressType, data)
}

public async detectHeaders(data: any) {  // ❌ data 可以是任何类型
  if (!this.headerDetector) {
    throw new Error('表头检测器未启用')
  }
  return this.headerDetector.detect(data)
}
```

**建议**: 添加输入验证和类型检查

#### 5.2.4 进程通信无超时 🟡 中危

**问题**:
```typescript
// header-detector.ts:88
const python = spawn(this.pythonPath, ['-m', 'security.header_detect'], {
  cwd: process.cwd(),
})
// ❌ 没有超时机制，Python 进程可能永久挂起
```

**建议**: 添加超时和重试机制

#### 5.2.5 错误信息泄露 🟢 低危

**问题**:
```typescript
// egress-switch.ts:142
return {
  allowed: false,
  reason: `拦截检查失败: ${error instanceof Error ? error.message : '未知错误'}`,
  // ⚠️ 可能泄露内部实现细节
}
```

**建议**: 使用通用错误消息，详细信息仅记录日志

### 5.3 密钥管理 ✅

**评估**: 良好

**优点**:
- ✅ 使用环境变量存储敏感信息
- ✅ .env.example 不包含真实密钥
- ✅ Python 使用 HMAC 签名密钥

**问题**:
- ⚠️ 缺少密钥轮换机制
- ⚠️ 没有密钥过期策略

---

## 6. 测试审计

### 6.1 测试覆盖率

| 模块 | 单元测试 | 集成测试 | E2E 测试 |
|------|---------|---------|---------|
| EgressSwitch | ✅ 10/10 | ❌ | ❌ |
| ListingTemplate | ✅ 9/9 | ❌ | ❌ |
| EdcFieldDetector | ✅ 7/7 | ❌ | ❌ |
| Service | ✅ 8/8 | ❌ | ❌ |
| HeaderDetector | ❌ 0/0 | ❌ | ❌ |
| auth 插件 | ❌ 0/0 | ❌ | ❌ |

**总计**: 34/34 TypeScript 单元测试通过

### 6.2 测试质量 ⚠️

**优点**:
- ✅ 测试覆盖核心逻辑
- ✅ 使用 Vitest 现代测试框架
- ✅ 测试代码清晰

**问题**:

1. **测试只验证 API，不验证实际行为**
```typescript
// 测试通过，但实际功能未实现
test('应该能检查 SAS 数据集出域', async () => {
  const result = await service.checkEgress(EgressType.SAS_DATASET, mockData)
  expect(result.allowed).toBe(true)  // ❌ 永远是 true，因为未实现
})
```

2. **缺少集成测试**
   - ❌ 没有测试 TypeScript ↔ Python 集成
   - ❌ 没有测试完整的数据流
   - ❌ 没有测试错误场景

3. **缺少性能测试**
   - ❌ 没有测试大数据量场景
   - ❌ 没有测试并发场景
   - ❌ 没有测试内存泄漏

---

## 7. 性能审计

### 7.1 潜在性能问题

#### 7.1.1 同步排序 ⚠️

**位置**: `listing-template.ts:263-273`

```typescript
// ⚠️ 大数据集可能阻塞事件循环
if (template.sortBy && template.sortBy.length > 0) {
  filteredData.sort((a, b) => {
    for (const col of template.sortBy!) {
      const aVal = a[col]
      const bVal = b[col]
      if (aVal < bVal) return -1
      if (aVal > bVal) return 1
    }
    return 0
  })
}
```

**建议**: 对大数据集使用 Worker 或分块处理

#### 7.1.2 Python 进程频繁创建 ⚠️

**位置**: `header-detector.ts:89`

```typescript
// ⚠️ 每次调用都 spawn 新进程
const python = spawn(this.pythonPath, ['-m', 'security.header_detect'], {
  cwd: process.cwd(),
})
```

**建议**: 使用进程池或长期运行的 Python 服务

#### 7.1.3 缺少缓存 ⚠️

**问题**: 
- 模板每次都从头加载
- EDC 识别结果未缓存
- 没有使用内存缓存

**建议**: 添加适当的缓存层

---

## 8. 可维护性审计

### 8.1 优点 ✅

1. **良好的代码组织**
   - ✅ 清晰的模块划分
   - ✅ 一致的命名约定
   - ✅ 完整的 TypeScript 类型

2. **完整的文档**
   - ✅ README 完整
   - ✅ JSDoc 注释
   - ✅ 多份设计文档

3. **现代工具链**
   - ✅ TypeScript 6.0
   - ✅ pnpm workspace
   - ✅ Vitest 测试框架

### 8.2 问题 ⚠️

1. **TODO 标记过多** ❌

统计结果:
```
packages/enterprise/auth/src/index.ts:38:    // TODO: 实现具体认证逻辑
packages/enterprise/clinical-guard/src/core/header-detector.ts:71:    // TODO: 实现更复杂的规则
packages/enterprise/clinical-guard/src/core/listing-template.ts:182:    // TODO: 从文件加载自定义模板
packages/enterprise/clinical-guard/src/core/egress-switch.ts:182:    // TODO: 实现 SAS 数据集拦截逻辑
packages/enterprise/clinical-guard/src/core/egress-switch.ts:192:      // TODO: 调用 AI 模型
packages/enterprise/clinical-guard/src/core/egress-switch.ts:211:    // TODO: 实现 Spec 数据拦截逻辑
packages/enterprise/clinical-guard/src/core/egress-switch.ts:221:      // TODO: 调用 AI 模型
```

**总计**: 7 个 TODO，其中 4 个是核心功能

2. **TypeScript 配置问题** ❌

```bash
# 根目录编译失败
$ npx tsc --noEmit
error TS18003: No inputs were found in config file
```

3. **auth 插件编译失败** ❌

```bash
$ cd packages/enterprise/auth && npx tsc --noEmit
src/index.ts(1,19): error TS2305: Module '"@deepseek-ai/cordis"' has no exported member 'Schema'.
src/index.ts(23,34): error TS2554: Expected 2 arguments, but got 3.
```

4. **版本控制混乱** ⚠️

Git 提交记录显示:
- 多次"最终交付"提交
- 提交信息不一致
- 包含"诚实"、"真实状态"等字眼，暗示之前的状态描述不准确

---

## 9. 依赖审计

### 9.1 直接依赖

| 包名 | 版本 | 用途 | 风险 |
|------|------|------|------|
| @deepseek-ai/dsh | 0.1.1-rc.2 | DSH 框架 | 🟡 RC 版本 |
| @deepseek-ai/cordis | 4.0.1 | 插件框架 | ✅ 稳定 |
| typescript | 6.0.0 | 编译器 | ✅ 最新 |
| vitest | 4.1.8 | 测试框架 | ✅ 最新 |

### 9.2 依赖问题

1. **使用 RC 版本** 🟡

`@deepseek-ai/dsh@0.1.1-rc.2` 是候选版本，可能不稳定

**建议**: 等待正式版或锁定版本

2. **版本兼容性问题** ❌

auth 插件使用了已移除的 API (`Schema` 导出)

**建议**: 修复 API 调用或降级依赖版本

3. **间接依赖数量** ⚠️

总计 490 个包，依赖树较深

**建议**: 定期审计依赖，移除未使用的包

---

## 10. 关键缺陷汇总

### 10.1 P0 - 致命缺陷 🔴

| ID | 类别 | 描述 | 影响 |
|----|------|------|------|
| P0-1 | 功能 | `interceptSasDataset` 未实现，永远返回 true | 数据安全开关失效 |
| P0-2 | 功能 | `interceptSpecData` 未实现，永远返回 true | 数据安全开关失效 |
| P0-3 | 安全 | 认证插件完全未实现 | 无访问控制 |
| P0-4 | 构建 | auth 插件编译失败 | 无法使用 |

### 10.2 P1 - 严重缺陷 🟡

| ID | 类别 | 描述 | 影响 |
|----|------|------|------|
| P1-1 | 功能 | 表头检测规则未实现 | 功能不完整 |
| P1-2 | 功能 | 自定义模板加载未实现 | 功能不完整 |
| P1-3 | 安全 | 输入验证缺失 | 可能崩溃或注入 |
| P1-4 | 性能 | Python 进程无超时 | 可能永久挂起 |
| P1-5 | 测试 | 缺少集成测试 | 集成风险 |

### 10.3 P2 - 一般缺陷 🟢

| ID | 类别 | 描述 | 影响 |
|----|------|------|------|
| P2-1 | 构建 | 根目录 TypeScript 配置错误 | 开发不便 |
| P2-2 | 性能 | 同步排序阻塞事件循环 | 大数据慢 |
| P2-3 | 性能 | 缺少缓存机制 | 性能未优化 |
| P2-4 | 可维护 | TODO 标记过多 | 代码不完整 |
| P2-5 | 安全 | 错误信息可能泄露细节 | 信息泄露 |

---

## 11. 改进建议

### 11.1 立即修复（P0）

1. **实现核心拦截逻辑**
   - 完成 `interceptSasDataset` 实现
   - 完成 `interceptSpecData` 实现
   - 添加实际的数据检查逻辑
   - 集成 AI 模型调用

2. **修复认证插件**
   - 移除 `Schema` 导入或使用正确的 API
   - 实现基础认证逻辑
   - 添加会话管理

3. **修复编译问题**
   - 修复 auth 插件的 TypeScript 错误
   - 修复根目录 tsconfig.json 配置

### 11.2 短期改进（P1）

1. **完善功能实现**
   - 实现表头检测规则
   - 实现自定义模板加载
   - 添加 AI 模型集成

2. **加强安全**
   - 添加输入验证
   - 实现超时机制
   - 统一错误处理

3. **补充测试**
   - 添加集成测试
   - 添加 E2E 测试
   - 测试错误场景

### 11.3 长期优化（P2）

1. **性能优化**
   - 实现缓存层
   - 使用 Python 进程池
   - 异步化大数据处理

2. **提高可维护性**
   - 清理所有 TODO
   - 统一代码风格
   - 完善文档

3. **工程化改进**
   - 添加 CI/CD
   - 自动化测试
   - 代码质量检查

---

## 12. 风险评估

### 12.1 整体风险矩阵

| 风险类别 | 风险等级 | 描述 |
|---------|---------|------|
| **功能完整性** | 🔴 高 | 核心拦截功能未实现 |
| **安全性** | 🔴 高 | 认证缺失，拦截失效 |
| **稳定性** | 🟡 中 | 部分代码未充分测试 |
| **性能** | 🟡 中 | 存在性能瓶颈 |
| **可维护性** | 🟡 中 | 代码质量参差不齐 |

### 12.2 上线风险评估

**当前状态**: ❌ **不建议上线**

**原因**:
1. 核心安全功能未实现（拦截逻辑）
2. 认证系统不可用
3. 缺少充分的集成测试

**建议**:
- 完成 P0 缺陷修复
- 补充集成测试
- 进行安全审计
- 小范围试点后再全面上线

---

## 13. 总结

### 13.1 项目优点

1. ✅ **架构设计合理**: 插件化、模块化设计清晰
2. ✅ **代码组织良好**: 文件结构清晰，命名规范
3. ✅ **类型安全**: TypeScript 严格模式，完整类型定义
4. ✅ **部分功能完整**: Listing 模板、EDC 识别测试通过
5. ✅ **文档完善**: 多份文档，JSDoc 注释

### 13.2 关键问题

1. ❌ **核心功能未实现**: 数据拦截逻辑是占位符
2. ❌ **认证系统缺失**: 无访问控制
3. ❌ **测试不充分**: 缺少集成和 E2E 测试
4. ❌ **编译问题**: auth 插件无法编译
5. ⚠️ **TODO 过多**: 7 个核心功能标记为 TODO

### 13.3 最终建议

**短期**（1-2 周）:
1. 实现核心拦截逻辑（P0-1, P0-2）
2. 修复认证插件（P0-3, P0-4）
3. 添加集成测试（P1-5）

**中期**（1-2 月）:
1. 完善所有功能实现
2. 补充安全机制
3. 性能优化

**长期**（3-6 月）:
1. 持续优化
2. 功能扩展
3. 生产化部署

---

**审计结论**: 项目架构设计良好，但核心功能实现不完整。建议完成 P0/P1 缺陷修复后再考虑上线。

**审计人**: Claude  
**审计日期**: 2026-08-25  
**下次审计建议**: 完成改进后 2-4 周

