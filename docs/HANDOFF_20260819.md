# Emerald Clinical Data Guard — 修复交接文件
**日期**: 2026-08-19  **当前状态**: 第 3 批完成,第 4 批待做  
**验证基线**: `run_all.py` 42/42, 集成 16/16, mutation 10/10

---

## 已完成批次

### 第 1 批(本轮前已完成)
E2E-1 worker UTF-8, E2E-3 GLM maxTokens, E2E-2 审计 try/finally(已在checkpoint实现)

### 第 2 批(本轮完成)
- ST-P1-1 全套模式 IGNORECASE(Python+Node 同步)
- ST-P1-2 NFKC + _INVISIBLE_RE 扩展(BOM/软连字符/bidi)
- ST-P1-5 开关白名单式 fail-closed(shadow/disabled 才不阻断)
- ST-P1-6 quickGuard safePatterns 改全等 `^...$`
- ST-P1-7 `_safe_key` 白名单式,中文/未知键哈希,删死代码 _KEY_DLP_RES
- ST-P1-3 authorize/consume 排他文件锁(TOCTOU)
- ST-P1-4 缺 user/session 拒绝授权/消费(不共享 anonymous 桶)

### 第 3 批(本轮完成)
- ST-D-4 审计月份硬编码改动态 `datetime.now().strftime('%Y%m')`
- 本地凭据通道: `credentialsDir`/`EMERALD_CREDENTIALS_DIR` 配置,isCredentialPath(防穿越),CREDENTIAL_LOCAL_ONLY 占位,spec 同步
- ST-P1-8 AI代码检查: `_DANGEROUS_BUILTINS`(eval/exec/compile/__import__/getattr), `_DANGEROUS_MODULES` 扩展(marshal/shelve/dill/joblib), `__import__(...).load` AST 检测, bash 引号拼接去混淆
- ST-P1-9 extractor 表头白名单输出: `_looks_like_data_value` 兜底(纯数值/数字占比≥40%的短码 REDACTED)
- ST-P2-3 env 白名单: worker+extractor spawn 改最小 env(PATH等必要项+显式传 EMERALD_*),删 `...process.env`

---

## 第 4 批 — 状态更新(2026-08-19 第二次会话)

### 已完成
| 缺陷 | 修复文件 | 说明 |
|---|---|---|
| **E2E-4** | `security/egress_checkpoint.py` | `_METADATA_KEY_FIELDS` 白名单 + line303 skip 已在代码中落地，补回归用例 `message_id_uuid_does_not_trigger_egress_block` |
| **ST-P2-6** | `security/ai_operations_monitor.py` | `_assess_file_threat` 增加 `_norm_path = file_path.replace('\\','/')` 归一化，补回归用例 `windows_path_separator_does_not_bypass_blacklist` |
| **ST-P2-9** | `src/branding.js` | walker 循环内增加 `data-clinical` 祖先检测，跳过临床数据容器内文本节点 |

### 本次新增完成
| 缺陷 | 修复文件 | 说明 |
|---|---|---|
| **ST-P0-1** | `README.md` + `package.json` + `var/` | 版本升 1.0.4→1.0.5，npm pack 重发布，README SHA-256 回标 `1D9A8147…`，旧包改为 .bak |
| **ST-P2-4** | `start.ps1` | `DSH_TELEMETRY_MODE` 改为无条件赋值 `'DISABLED'`，不再尊重外部预设 |

### 第三次会话新增完成
| 缺陷 | 修复文件 | 说明 |
|---|---|---|
| **ST-P2-13** | `security/egress_authz.py` + `security/ai_operations_monitor.py` | 默认目录改回项目内 `var/`，符合规范 §10.2 |
| **ST-P3-x** | `security/patterns.py` + `security/audit_log.py` + `security/egress_checkpoint.py` | stable_hash 加 HMAC 盐（EMERALD_HASH_SALT），审计行追加 fsync，单例改双重检查锁 |
| **ST-D-3** | `tests/unit/test_security.py` | 补授权重放/并发双消费/单例线程安全三个回归用例 |

### 待做
| 优先 | 缺陷 | 描述 |
|---|---|---|
| 续跑 | **E2E-7** | listing 全流程 E2E 续跑（dsh-guard 修复全部完成，可开新会话执行） |

**当前测试基线**: TOTAL_FAILED_SUITES=0（run_all.py unit+integration+bypass+resilience）。所有 P0-P3 缺陷已修完。

---

## 接续方法

新会话: "读 `G:\home\dsh-guard\docs\HANDOFF_20260819.md`，继续 E2E-7。仓库在 `G:\home\dsh-guard`。"

当前验证基线: TOTAL_FAILED_SUITES=0（run_all.py unit+integration+bypass+resilience），mutation 10/10。所有 P0-P3 缺陷已修完。

**E2E-7 状态**: ALL_PASS（6/6 场景全绿，2026-08-19 完成）。所有缺陷已全部修完，无待做项。
